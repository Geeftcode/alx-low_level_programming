#ifndef SUM(x, y)
#define SUM(x, y) ((x) + (y))
/*
 * macro created to sum x and y
 * return the value of sum
 */
int main ()
{
	int i;

	i = SUM(20, 300);
	printf(i = "%d\n", i);
	return (0);
}
#endif
